import { appNamespace } from '@/_constants/i18next/app-namespace';
import { PAGE_SUB_NAMESPACE } from './page-sub-namespace';

export const TODO_PAGE_TRANSLATIONS = {
  title: `${appNamespace}:${PAGE_SUB_NAMESPACE}.title`,
  placeholder: `${appNamespace}:${PAGE_SUB_NAMESPACE}.placeholder`,
};
